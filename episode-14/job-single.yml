apiVersion: batch/v1
kind: Job
metadata:
  name: single-job
spec:
  template:
    spec:
      containers:
        - name: task
          image: busybox
          command: ["echo", "Single task completed"]
      restartPolicy: Never
