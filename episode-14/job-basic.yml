apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
spec:
  template:
    spec:
      containers:
        - name: hello
          image: busybox
          command:
            - /bin/sh
            - -c
            - echo "Hello from Kubernetes Job!"; sleep 5; echo "Job completed!"
      restartPolicy: Never
