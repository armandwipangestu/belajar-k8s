apiVersion: batch/v1
kind: CronJob
metadata:
  name: sequential-cronjob
spec:
  schedule: "*/1 * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: task
              image: busybox
              args:
                - /bin/sh
                - -c
                - echo "Starting"; sleep 120; echo "Done"
          restartPolicy: Never
